uname -sr 内核版本查看

电信宽带密码d7rtzwnr
zip文件乱码问题
unzip -O cp936 xxx

ctrl+alt+P截图(自定义)
tar zxvf xxx 解压
tar zcvf FileName.tar.Z DirName 压缩

rar x xxx

sudo service mongod start  # 启动
sudo service mongod stop   # 关闭
ps aux | grep mongod   # 查看守护进程mongod的运行状态
关闭守护进程mongod
sudo service mongod stop
安装git
sudo apt-get update  
sudo apt-get install git
配置 你的github
git config --global user.name "noatnu"
git config --global user.email "noatnu@163.com"
查看配置信息
ssh -v git@github.com
git config --list
编辑配置信息
gedit ~/.gitconfig
ssh-keygen -C 'noatnu@163.com' -t rsa

到/root/.ssh下 copy id_rsa.pub

关闭端口
iptables -A INPUT -p tcp --dport 8080 -j DROP

打开端口

iptables -A INPUT -p tcp --dport 8090 -j ACCEPT
